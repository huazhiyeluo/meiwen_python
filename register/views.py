from django.shortcuts import render

# Create your views here.
from django.shortcuts import render

# Create your views here.
def register():
    data          = {}
    return render(request, 'index/info.html', data)

def registerdo():
    data          = {}
    return render(request, 'index/info.html', data)